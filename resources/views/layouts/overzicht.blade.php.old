@extends('base')

@section('bodyy')
  <div class="menu">
    <div class="pattient">
      {{ Form::open(['url' =>  '/dossier', 'method' => 'post', 'class' => 'form-layout']) }}
      {{ Form::input('text', 'inp', null, ["placeholder" => "Naam patiënt"]) }}
      {{ Form::submit("send", ["class" => 'hidden']) }}
      {{ Form::close() }}
      <div class="settings">
        <a href="#" data-toggle="modal" data-target="#settings">
          <img src="/img/settings.png">
        </a>
      </div>
    </div>
  </div>
  <div class="overview">
    <div class="header">
      <div class="title">
        Afdelingen
      </div>
      <div class="header_row">
        @foreach($departments as $department)
          <div class="block">
            <a href="{{$department->id}}" class="inner {{ Request::is($department->id) ? 'active' : '' }}">
              {{ $department->short_name }}
            </a>
          </div>
        @endforeach
      </div>
    </div>

    <div class="content">
        <div class="description">
          <div class="title bold capitalize">
            {{ $departments[$nr]->name }}
          </div>
          @foreach($rooms as $room)
            @if($room->department_id == $departments[$nr]->id)
            <ul>
              <li>{{ $room->description }}</li>
              <li>
                {{-- vol / empty --}}
                vol
              </li>
              <li>kamer vrij over 1 uur 22 minuten</li>
              <li class="warning">Let op!</li>
            </ul>
            @endif
          @endforeach
      </div>
    </div>

    <div class="bed">
      @foreach($hospital_beds as $hbed)
        @if(strpos($hbed->name, 'b01') !== false)
          <div class="clear" style="clear:both;"></div>
        @endif
        <div class="row_block room_{{$hbed->room}}">
          <label for="{{ $hbed->name }}" class="radiobutton {{-- checked --}}">
          <input type="radio" name="patient" id="{{ $hbed->name }}" {{-- class="hidden"--}}></label>
          <div class="status color_{{$hbed->status}}"></div>
          <div class="number">{{ $hbed->name }}</div>
        </div>
      @endforeach
    </div>
  </div>

@endsection

@push('customscripts')
  <script>

  </script>
@endpush
